import { StyleSheet } from 'react-native';

/** @param {import('@/data/constants/themes').AppThemeColors} c */
export function createRouteDetailStyles(c) {
  const isDark = c.mode === 'dark';

  return StyleSheet.create({
  safe: { flex: 1, backgroundColor: c.pageBg },
  scroll: { paddingHorizontal: 18, paddingBottom: 124 },
  hero: {
    backgroundColor: c.heroBg,
    borderRadius: 18,
    padding: 14,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 10,
    marginBottom: 10,
  },
  heroIcon: {
    width: 36,
    height: 36,
    borderRadius: 12,
    backgroundColor: 'rgba(255,255,255,0.2)',
    alignItems: 'center',
    justifyContent: 'center',
  },
  heroTitle: { color: '#fff', fontSize: 19, fontWeight: '800' },
  heroSub: { color: '#dbeafe', fontSize: 12, marginTop: 2 },
  dateRow: { flexDirection: 'row', gap: 8, marginBottom: 10 },
  dateChip: {
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 999,
    paddingHorizontal: 14,
    paddingVertical: 8,
    backgroundColor: c.card,
  },
  dateChipActive: { backgroundColor: '#dbeafe', borderColor: '#93c5fd' },
  dateChipText: { fontSize: 12, color: c.textMuted, fontWeight: '700' },
  dateChipTextActive: { color: '#1e3a8a' },
  panel: {
    backgroundColor: c.card,
    borderRadius: 18,
    borderWidth: 1,
    borderColor: c.borderLight,
    padding: 14,
  },
  adminTabContentPanel: { marginTop: 16 },
  panelTitle: { fontSize: 17, fontWeight: '800', color: c.text },
  currentStatusTitleRow: { flexDirection: 'row', alignItems: 'center', gap: 8, marginBottom: 8 },
  currentStatusTitleText: { fontSize: 19, fontWeight: '900', color: c.text },
  currentStatusChipRow: { flexDirection: 'row', gap: 10 },
  currentStatusChip: {
    flex: 1,
    minHeight: 52,
    borderRadius: 16,
    borderWidth: 1.5,
    borderColor: isDark ? c.borderLight : '#cfd5e5',
    backgroundColor: isDark ? c.surfaceElevated : '#f4f6fd',
    alignItems: 'center',
    justifyContent: 'center',
    paddingHorizontal: 10,
  },
  currentStatusChipHover: {
    borderColor: isDark ? '#facc15' : '#facc15',
    backgroundColor: isDark ? '#422006' : '#fffbeb',
  },
  currentStatusChipPresent: {
    borderColor: isDark ? '#22c55e' : '#6ee7b7',
    borderWidth: 3,
    backgroundColor: isDark ? '#052e16' : '#ecfdf5',
  },
  currentStatusChipAbsent: {
    borderColor: isDark ? '#818cf8' : '#a78bfa',
    borderWidth: 3,
    backgroundColor: isDark ? '#1e1b4b' : '#f5f3ff',
  },
  currentStatusChipLeave: {
    borderColor: isDark ? '#fb7185' : '#f9a8d4',
    borderWidth: 3,
    backgroundColor: isDark ? '#450a0a' : '#fff1f2',
  },
  currentStatusChipText: {
    fontSize: 12,
    color: isDark ? c.textMuted : '#475569',
    fontWeight: '800',
    letterSpacing: 1.1,
  },
  currentStatusChipTextActive: { color: isDark ? c.text : c.text },
  panelSub: { marginTop: 3, marginBottom: 10, fontSize: 12, color: c.textMuted, lineHeight: 18 },
  adminGrid: { gap: 10 },
  adminPanelTabsWrap: {
    marginTop: 10,
    borderWidth: 1,
    borderColor: c.borderStrong,
    borderRadius: 18,
    backgroundColor: c.card,
    paddingVertical: 12,
    paddingHorizontal: 10,
  },
  adminPanelTabsRow: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: 14,
  },
  adminPanelTab: {
    flex: 1,
    minHeight: 64,
    paddingVertical: 8,
    paddingHorizontal: 6,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: 'transparent',
    overflow: 'hidden',
  },
  adminPanelTabIconWrap: {
    width: 52,
    height: 52,
    borderRadius: 16,
    alignItems: 'center',
    justifyContent: 'center',
  },
  adminPanelTabIndicator: {
    position: 'absolute',
    left: 12,
    right: 12,
    bottom: 0,
    height: 4,
    borderTopLeftRadius: 4,
    borderTopRightRadius: 4,
  },
  adminCard: {
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 16,
    backgroundColor: c.surfaceMuted,
    padding: 12,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 10,
  },
  adminFilterCard: {
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 14,
    backgroundColor: c.surfaceMuted,
    padding: 10,
    marginTop: 8,
  },
  adminSectionTitle: { marginTop: 12, marginBottom: 2, fontSize: 17, fontWeight: '800', color: c.text },
  adminUserCard: {
    borderWidth: 1,
    borderColor: c.borderStrong,
    borderRadius: 14,
    backgroundColor: c.card,
    padding: 12,
    marginTop: 8,
    borderTopWidth: 3,
    borderTopColor: '#6366f1',
  },
  adminUserTop: { flexDirection: 'row', alignItems: 'center', gap: 10 },
  adminMemberAvatar: {
    width: 40,
    height: 40,
    borderRadius: 20,
    backgroundColor: isDark ? '#172554' : '#dbeafe',
    alignItems: 'center',
    justifyContent: 'center',
  },
  adminMemberAvatarText: { fontSize: 16, fontWeight: '800', color: isDark ? '#93c5fd' : '#1d4ed8' },
  adminMemberName: { fontSize: 15, fontWeight: '800', color: c.text },
  adminMemberEmail: { marginTop: 1, fontSize: 12, fontWeight: '600', color: c.textMuted },
  adminMemberMeta: { marginTop: 8, fontSize: 12, fontWeight: '700', color: c.textMuted },
  adminAwaitingText: { marginTop: 4, fontSize: 12, color: isDark ? '#c7d2fe' : '#4338ca', fontWeight: '700' },
  adminTagRow: { marginTop: 9, flexDirection: 'row', flexWrap: 'wrap', gap: 6 },
  adminTagPill: {
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 999,
    backgroundColor: c.chipBg,
    paddingHorizontal: 8,
    paddingVertical: 4,
  },
  adminTagText: { fontSize: 10, color: c.textMuted, fontWeight: '800', textTransform: 'uppercase' },
  adminTagPending: {
    backgroundColor: isDark ? '#1e1b4b' : '#eef2ff',
    borderColor: isDark ? '#4338ca' : '#c7d2fe',
  },
  adminTagPendingText: { color: isDark ? '#c7d2fe' : '#4338ca' },
  adminTagActive: {
    backgroundColor: isDark ? '#052e16' : '#dcfce7',
    borderColor: isDark ? '#166534' : '#bbf7d0',
  },
  adminTagActiveText: { color: isDark ? '#86efac' : '#166534' },
  adminTagId: {
    backgroundColor: isDark ? '#172554' : '#eff6ff',
    borderColor: isDark ? '#1d4ed8' : '#bfdbfe',
  },
  adminTagIdText: { fontSize: 10, color: isDark ? '#93c5fd' : '#1d4ed8', fontWeight: '800' },
  adminActionRow: {
    marginTop: 12,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: 10,
  },
  adminPromoteBtn: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 6,
    backgroundColor: '#2563eb',
    borderRadius: 10,
    paddingHorizontal: 12,
    paddingVertical: 8,
  },
  adminPromoteText: { color: '#fff', fontSize: 12, fontWeight: '800' },
  adminDeleteBtn: {
    width: 34,
    height: 34,
    borderRadius: 10,
    borderWidth: 1,
    borderColor: c.dangerBorder,
    backgroundColor: isDark ? '#450a0a' : '#fff1f2',
    alignItems: 'center',
    justifyContent: 'center',
  },
  adminRejectBtn: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 6,
    borderRadius: 10,
    paddingHorizontal: 12,
    paddingVertical: 8,
    borderWidth: 1,
    borderColor: '#ef4444',
    backgroundColor: c.card,
  },
  adminRejectText: { color: '#dc2626', fontSize: 12, fontWeight: '800' },
  adminRuleRow: {
    borderWidth: 1,
    borderColor: c.borderStrong,
    borderRadius: 12,
    backgroundColor: c.surfaceMuted,
    paddingHorizontal: 12,
    paddingVertical: 10,
    marginTop: 8,
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    gap: 10,
  },
  adminRuleLabel: { fontSize: 13, color: c.textMuted, fontWeight: '700' },
  adminRuleValue: { fontSize: 13, color: c.text, fontWeight: '800' },
  adminTimeSummary: { flexDirection: 'row', gap: 8, marginTop: 10 },
  adminTimeBox: {
    flex: 1,
    borderWidth: 1,
    borderColor: isDark ? '#1d4ed8' : '#bfdbfe',
    borderRadius: 12,
    backgroundColor: isDark ? '#172554' : '#eff6ff',
    paddingVertical: 10,
    alignItems: 'center',
  },
  adminTimeBoxLabel: { fontSize: 11, color: isDark ? '#93c5fd' : '#1e3a8a', fontWeight: '700' },
  adminTimeBoxValue: { marginTop: 3, fontSize: 17, color: isDark ? '#93c5fd' : '#1e3a8a', fontWeight: '900' },
  adminDeptCard: {
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 14,
    backgroundColor: c.surfaceMuted,
    padding: 12,
    marginTop: 8,
  },
  adminDeptTop: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', gap: 10 },
  adminDeptName: { fontSize: 15, fontWeight: '800', color: c.text },
  adminDeptLeads: { fontSize: 12, fontWeight: '700', color: '#0d9488' },
  adminDeptCount: { marginTop: 6, fontSize: 13, fontWeight: '700', color: c.textMuted },
  deptHeaderRow: {
    flexDirection: 'row',
    alignItems: 'flex-start',
    justifyContent: 'space-between',
    gap: 12,
  },
  deptHeaderTextCol: { flex: 1, minWidth: 0 },
  deptHeaderTitle: { fontSize: 22, fontWeight: '800', color: c.text, letterSpacing: -0.3 },
  deptHeaderSub: { marginTop: 4, fontSize: 13, color: c.textMuted, fontWeight: '500', lineHeight: 18 },
  deptTotalBadge: {
    borderRadius: 10,
    backgroundColor: isDark ? '#052e16' : '#dcfce7',
    paddingHorizontal: 10,
    paddingVertical: 6,
  },
  deptTotalBadgeText: { fontSize: 12, fontWeight: '800', color: isDark ? '#86efac' : '#166534' },
  deptNameInputWrap: {
    marginTop: 16,
    borderWidth: 1,
    borderColor: c.borderStrong,
    borderRadius: 14,
    backgroundColor: c.card,
    paddingHorizontal: 14,
  },
  deptNameInput: { paddingVertical: 12, fontSize: 14, color: c.text },
  deptAddPrimaryBtn: {
    marginTop: 14,
    width: '100%',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 8,
    borderRadius: 14,
    paddingVertical: 14,
    paddingHorizontal: 16,
    backgroundColor: '#059669',
  },
  deptAddPrimaryText: { color: '#ffffff', fontSize: 16, fontWeight: '800' },
  deptRow: {
    borderWidth: 1,
    borderColor: c.borderStrong,
    borderRadius: 14,
    backgroundColor: c.surfaceMuted,
    paddingHorizontal: 14,
    paddingVertical: 10,
    marginTop: 10,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
  deptName: { fontSize: 14, fontWeight: '700', color: c.text },
  deptRemoveBtn: {
    width: 40,
    height: 40,
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 999,
    backgroundColor: isDark ? '#450a0a' : '#fef2f2',
  },
  timeControlStack: { marginTop: 12, gap: 16 },
  timeControlFormBlock: { gap: 14 },
  timeHeroCard: {
    borderRadius: 20,
    backgroundColor: isDark ? '#431407' : '#fff7ed',
    paddingVertical: 16,
    paddingHorizontal: 14,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 12,
  },
  timeHeroTextCol: { flex: 1, minWidth: 0 },
  timeHeroTitle: { fontSize: 16, fontWeight: '800', color: c.text, letterSpacing: -0.2 },
  timeHeroSub: { marginTop: 4, fontSize: 13, color: c.textMuted, fontWeight: '500', lineHeight: 18 },
  timeFormRow: { flexDirection: 'row', gap: 12, marginTop: 2 },
  timeField: { flex: 1 },
  timeFieldHalf: { flex: 1 },
  timeFieldLabel: { fontSize: 11, fontWeight: '700', color: c.textMuted, marginBottom: 4 },
  timeInputWrap: {
    flexDirection: 'row',
    alignItems: 'center',
    borderWidth: 1,
    borderColor: c.inputBorder,
    borderRadius: 12,
    backgroundColor: c.inputBg,
    paddingHorizontal: 10,
  },
  timeInput: {
    flex: 1,
    paddingVertical: 8,
    paddingRight: 8,
    fontSize: 13,
    color: c.text,
  },
  timeSaveRow: { marginTop: 4, width: '100%', alignSelf: 'stretch' },
  timeSaveBtn: {
    width: '100%',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 10,
    borderRadius: 14,
    paddingVertical: 14,
    paddingHorizontal: 16,
    backgroundColor: '#f97316',
  },
  timeSaveText: { color: '#ffffff', fontSize: 15, fontWeight: '800' },
  adminPrimaryBtnDisabled: { opacity: 0.7 },
  adminRoleModal: {
    width: '90%',
    maxWidth: 380,
    borderRadius: 24,
    backgroundColor: c.card,
    paddingHorizontal: 20,
    paddingTop: 18,
    paddingBottom: 20,
    shadowcolor: c.text,
    shadowOffset: { width: 0, height: 16 },
    shadowOpacity: 0.14,
    shadowRadius: 28,
    elevation: 12,
  },
  adminRoleModalHead: {
    flexDirection: 'row',
    alignItems: 'flex-start',
    justifyContent: 'space-between',
  },
  adminRoleModalIcon: {
    width: 48,
    height: 48,
    borderRadius: 24,
    backgroundColor: isDark ? '#172554' : '#dbeafe',
    alignItems: 'center',
    justifyContent: 'center',
  },
  adminRoleModalClose: {
    width: 36,
    height: 36,
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 2,
  },
  adminRoleTitle: { marginTop: 14, fontSize: 20, fontWeight: '800', color: c.text, letterSpacing: -0.2 },
  adminRoleUserName: { marginTop: 8, fontSize: 17, color: c.text, fontWeight: '800' },
  adminRoleUserId: { marginTop: 4, fontSize: 13, color: c.textSecondary, fontWeight: '600' },
  adminRoleOptionsList: { marginTop: 16, gap: 10 },
  adminRoleOption: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 12,
    borderRadius: 14,
    backgroundColor: isDark ? c.surfaceElevated : '#f1f5f9',
    paddingVertical: 14,
    paddingHorizontal: 14,
    borderWidth: 1.5,
    borderColor: 'transparent',
  },
  adminRoleOptionSelected: {
    backgroundColor: isDark ? '#172554' : '#eff6ff',
    borderColor: c.primaryMid,
  },
  adminRoleOptionIconWrap: {
    width: 36,
    height: 36,
    borderRadius: 10,
    backgroundColor: isDark ? '#334155' : '#e2e8f0',
    alignItems: 'center',
    justifyContent: 'center',
  },
  adminRoleOptionIconWrapSelected: {
    backgroundColor: isDark ? '#1e3a8a' : '#dbeafe',
  },
  adminRoleOptionText: { flex: 1, fontSize: 15, color: c.text, fontWeight: '800' },
  adminRoleOptionTextSelected: { color: c.primaryLight },
  adminRoleCancelBtn: {
    marginTop: 18,
    height: 48,
    borderRadius: 14,
    backgroundColor: isDark ? c.surfaceElevated : '#f1f5f9',
    alignItems: 'center',
    justifyContent: 'center',
  },
  adminRoleCancelBtnText: { fontSize: 15, color: c.textMuted, fontWeight: '800' },
  adminCardStatusIcon: { marginLeft: 4 },
  adminCardActive: { borderColor: c.chipActiveBorder, backgroundColor: isDark ? '#172554' : '#eef6ff' },
  adminIconWrap: {
    width: 42,
    height: 42,
    borderRadius: 14,
    alignItems: 'center',
    justifyContent: 'center',
  },
  adminCardTitle: { fontSize: 15, color: c.text, fontWeight: '800', lineHeight: 20 },
  input: {
    borderWidth: 1,
    borderColor: c.inputBorder,
    borderRadius: 12,
    backgroundColor: c.inputBg,
    color: c.text,
    paddingHorizontal: 10,
    paddingVertical: 10,
  },
  textArea: { minHeight: 150 },
  textAreaSm: { minHeight: 110 },
  actionBtn: {
    marginTop: 10,
    alignSelf: 'flex-start',
    borderRadius: 10,
    backgroundColor: c.primaryMid,
    paddingHorizontal: 14,
    paddingVertical: 10,
  },
  actionBtnText: { color: '#fff', fontSize: 13, fontWeight: '700' },
  filterRow: { marginBottom: 10, gap: 8 },
  searchWrap: {
    flexDirection: 'row',
    alignItems: 'center',
    borderWidth: 1,
    borderColor: c.inputBorder,
    borderRadius: 10,
    backgroundColor: c.searchBg,
    paddingHorizontal: 10,
  },
  searchInput: {
    flex: 1,
    color: c.text,
    fontSize: 13,
    paddingVertical: 9,
    paddingHorizontal: 8,
  },
  chipRow: { flexDirection: 'row', flexWrap: 'wrap', gap: 6 },
  adminRoleChipRow: { flexDirection: 'row', flexWrap: 'wrap', gap: 10, rowGap: 10 },
  filterChip: {
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 999,
    backgroundColor: c.chipBg,
    paddingHorizontal: 10,
    paddingVertical: 6,
  },
  filterChipActive: { backgroundColor: c.chipActiveBg, borderColor: c.chipActiveBorder },
  filterChipText: { fontSize: 11, color: c.textMuted, fontWeight: '700', textTransform: 'capitalize' },
  filterChipTextActive: { color: c.chipActiveText },
  rowItem: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    borderWidth: 1,
    borderColor: c.borderStrong,
    borderRadius: 10,
    paddingHorizontal: 10,
    paddingVertical: 9,
    marginBottom: 6,
  },
  rowName: { fontSize: 13, fontWeight: '600', color: c.text },
  rowStatus: { fontSize: 12, fontWeight: '700', color: '#4f46e5' },
  tlCard: {
    borderWidth: 1,
    borderColor: c.borderStrong,
    borderRadius: 10,
    backgroundColor: '#f8fafc',
    padding: 10,
    marginBottom: 8,
  },
  tlTeam: { fontSize: 13, fontWeight: '800', color: c.text },
  tlLead: { fontSize: 11, color: c.textMuted, marginTop: 2 },
  tlBody: { fontSize: 12, color: '#334155', marginTop: 5, lineHeight: 18 },
  statsRow: { flexDirection: 'row', gap: 8, marginBottom: 10 },
  statPill: {
    flex: 1,
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 12,
    paddingVertical: 10,
    backgroundColor: '#eff6ff',
    alignItems: 'center',
  },
  statPillLabel: { fontSize: 11, color: '#1e3a8a', fontWeight: '700' },
  statPillValue: { marginTop: 3, fontSize: 16, color: '#1e3a8a', fontWeight: '800' },
  hrNoteBox: {
    borderWidth: 1,
    borderColor: '#c4b5fd',
    borderRadius: 12,
    backgroundColor: '#f5f3ff',
    padding: 12,
    marginTop: 4,
  },
  hrNoteTitle: { fontSize: 12, color: '#6d28d9', fontWeight: '800' },
  hrNoteText: { fontSize: 12, color: '#4c1d95', marginTop: 5, lineHeight: 18 },
  dateFilterRow: { flexDirection: 'row', gap: 8, marginTop: 8 },
  dateInput: { flex: 1 },
  filterDateField: {
    flex: 1,
    minWidth: 0,
    flexDirection: 'row',
    alignItems: 'center',
    flexWrap: 'nowrap',
    gap: 8,
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 12,
    minHeight: 48,
    paddingVertical: 8,
    paddingHorizontal: 10,
    backgroundColor: c.card,
  },
  filterDateFieldSingle: {
    flex: 1,
    minWidth: 0,
    fontSize: 14,
    fontWeight: '700',
    color: c.text,
    lineHeight: 18,
  },
  filterDateFieldPlaceholder: { color: c.inputPlaceholder, fontWeight: '600' },
  filterDateClear: { alignSelf: 'flex-start', marginTop: 8, paddingVertical: 4 },
  filterDateClearText: { fontSize: 13, fontWeight: '700', color: c.primaryLight },
  projectCard: {
    flexDirection: 'row',
    overflow: 'hidden',
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 22,
    backgroundColor: c.card,
    marginBottom: 10,
    shadowcolor: c.text,
    shadowOffset: { width: 0, height: 5 },
    shadowOpacity: 0.07,
    shadowRadius: 10,
    elevation: 3,
  },
  projectDateStrip: {
    width: 78,
    backgroundColor: isDark ? '#172554' : '#dce2f2',
    alignItems: 'center',
    justifyContent: 'center',
    paddingVertical: 14,
  },
  projectCardCompact: { borderRadius: 18 },
  projectDateStripCompact: { width: 66 },
  projectDateDay: { fontSize: 24, color: isDark ? '#93c5fd' : '#0284c7', fontWeight: '900', lineHeight: 28 },
  projectDateMonth: {
    marginTop: 2,
    fontSize: 9,
    color: isDark ? '#93c5fd' : '#0284c7',
    fontWeight: '800',
    letterSpacing: 1.2,
  },
  projectMainCol: { flex: 1, minWidth: 0, paddingHorizontal: 14, paddingVertical: 14 },
  projectCardTop: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'flex-start', gap: 10, minWidth: 0 },
  projectTitle: { flex: 1, minWidth: 0, flexShrink: 1, fontSize: 17, fontWeight: '900', color: c.text },
  projectStatePill: {
    marginTop: 8,
    alignSelf: 'flex-start',
    flexDirection: 'row',
    alignItems: 'center',
    borderWidth: 1,
    borderRadius: 999,
    paddingHorizontal: 14,
    paddingVertical: 4,
  },
  projectStatusDefault: {
    borderColor: isDark ? '#475569' : '#cbd5e1',
    backgroundColor: isDark ? c.surfaceElevated : '#f8fafc',
  },
  projectStatusDefaultText: { color: isDark ? '#cbd5e1' : '#475569' },
  projectStatusPending: {
    borderColor: isDark ? '#854d0e' : '#fde68a',
    backgroundColor: isDark ? '#422006' : '#fff7d6',
  },
  projectStatusPendingText: { color: isDark ? '#fcd34d' : '#b45309' },
  projectStatusProgress: {
    borderColor: isDark ? '#1d4ed8' : '#bfdbfe',
    backgroundColor: isDark ? '#172554' : '#eff6ff',
  },
  projectStatusProgressText: { color: isDark ? '#93c5fd' : '#1d4ed8' },
  projectStatusReview: {
    borderColor: isDark ? '#6d28d9' : '#ddd6fe',
    backgroundColor: isDark ? '#2e1065' : '#f5f3ff',
  },
  projectStatusReviewText: { color: isDark ? '#c4b5fd' : '#6d28d9' },
  projectStatusSubmitted: {
    borderColor: isDark ? '#0f766e' : '#99f6e4',
    backgroundColor: isDark ? '#042f2e' : '#ecfeff',
  },
  projectStatusSubmittedText: { color: isDark ? '#5eead4' : '#0f766e' },
  projectStatusOverdue: {
    borderColor: isDark ? '#991b1b' : '#fecaca',
    backgroundColor: isDark ? '#450a0a' : '#fff1f2',
  },
  projectStatusOverdueText: { color: isDark ? '#fca5a5' : '#b91c1c' },
  projectStatusCompleted: {
    borderColor: isDark ? '#166534' : '#bbf7d0',
    backgroundColor: isDark ? '#052e16' : '#f0fdf4',
  },
  projectStatusCompletedText: { color: isDark ? '#86efac' : '#166534' },
  projectStateText: { fontSize: 12, fontWeight: '900', letterSpacing: 0.8 },
  projectIdentityRow: { marginTop: 8, flexDirection: 'row', alignItems: 'center', gap: 8 },
  projectAssigneeBadge: {
    borderRadius: 999,
    backgroundColor: isDark ? c.surfaceElevated : '#dce2f2',
    paddingHorizontal: 10,
    paddingVertical: 4,
  },
  projectAssigneeBadgeText: { fontSize: 12, fontWeight: '800', color: c.text },
  projectInfoLine: {
    marginTop: 8,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
    minWidth: 0,
    width: '100%',
  },
  projectInfoText: {
    flex: 1,
    flexShrink: 1,
    minWidth: 0,
    fontSize: 13,
    color: c.textMuted,
    fontWeight: '500',
  },
  projectDueLine: { marginTop: 8, flexDirection: 'row', alignItems: 'center', gap: 8 },
  projectDueText: { fontSize: 13, color: c.text, fontWeight: '800' },
  projectTagChip: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 4,
    borderWidth: 1,
    borderColor: '#cbd5e1',
    borderRadius: 999,
    paddingHorizontal: 8,
    paddingVertical: 4,
    backgroundColor: c.card,
  },
  projectAssigneeChip: { borderColor: '#cbd5e1', backgroundColor: '#f8fafc' },
  projectPriorityHigh: { borderColor: '#fecaca', backgroundColor: '#fff1f2' },
  projectPriorityMedium: { borderColor: '#fde68a', backgroundColor: '#fffbeb' },
  projectPriorityLow: { borderColor: '#bbf7d0', backgroundColor: '#f0fdf4' },
  projectTagText: { fontSize: 11, fontWeight: '700', color: '#334155' },
  projectMetaRow: { marginTop: 8, flexDirection: 'row', justifyContent: 'space-between', gap: 8 },
  projectMetaText: { fontSize: 11, color: c.textMuted, fontWeight: '700' },
  pmFilterSelectWrap: { marginTop: 8, position: 'relative', zIndex: 40 },
  pmFilterSelectBtn: {
    height: 48,
    borderRadius: 14,
    borderWidth: 1,
    borderColor: c.inputBorder,
    backgroundColor: c.inputBg,
    paddingHorizontal: 14,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
  pmFilterSelectText: { fontSize: 16, color: c.text, fontWeight: '700' },
  pmFilterSelectMenuInline: {
    position: 'absolute',
    left: 0,
    right: 0,
    top: 54,
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 14,
    backgroundColor: c.card,
    overflow: 'hidden',
    shadowcolor: c.text,
    shadowOffset: { width: 0, height: 10 },
    shadowOpacity: 0.18,
    shadowRadius: 16,
    elevation: 10,
  },
  pmFilterOption: { paddingHorizontal: 14, paddingVertical: 10 },
  pmFilterOptionActive: { backgroundColor: isDark ? '#172554' : '#eef2ff' },
  pmFilterOptionText: { fontSize: 14, color: c.textMuted, fontWeight: '600' },
  pmFilterOptionTextActive: { color: isDark ? '#93c5fd' : '#1d4ed8', fontWeight: '800' },
  projectFooter: { marginTop: 10, flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' },
  projectStatusChip: { backgroundColor: '#dbeafe', borderColor: '#93c5fd' },
  deleteBtn: {
    alignItems: 'center',
    justifyContent: 'center',
    width: 34,
    height: 34,
    borderRadius: 17,
    borderWidth: 1,
    borderColor: c.dangerBorder,
    backgroundColor: c.dangerBg,
  },
  deleteBtnText: { color: '#fff', fontSize: 12, fontWeight: '700' },
  emptyBox: {
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 12,
    backgroundColor: c.surfaceMuted,
    paddingVertical: 18,
    alignItems: 'center',
  },
  emptyText: { color: c.textMuted, fontSize: 12, fontWeight: '600' },
  modalOverlay: {
    flex: 1,
    backgroundColor: c.modalBackdrop,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 14,
  },
  dateOverlay: {
    flex: 1,
    backgroundColor: c.modalBackdrop,
    justifyContent: 'flex-end',
  },
  dateSheet: {
    backgroundColor: c.card,
    borderTopLeftRadius: 18,
    borderTopRightRadius: 18,
    paddingHorizontal: 16,
    paddingTop: 14,
    paddingBottom: 28,
    borderWidth: 1,
    borderColor: c.borderLight,
  },
  modalCardShell: {
    maxHeight: '86%',
    width: '100%',
    maxWidth: 560,
  },
  modalScroll: {
    maxHeight: '100%',
  },
  modalScrollContent: {
    flexGrow: 1,
    justifyContent: 'center',
    overflow: 'visible',
  },
  modalCard: {
    backgroundColor: c.card,
    borderRadius: 18,
    borderWidth: 1,
    borderColor: c.borderLight,
    padding: 14,
    gap: 8,
    overflow: 'visible',
  },
  modalTitle: { fontSize: 16, fontWeight: '800', color: c.text, marginBottom: 4 },
  formLabelRow: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
    marginTop: 12,
    marginBottom: 4,
    flexWrap: 'wrap',
  },
  formLabelRowTight: { marginTop: 8, marginBottom: 2 },
  formLabelUpper: {
    fontSize: 12,
    fontWeight: '800',
    color: c.text,
    letterSpacing: 0.6,
  },
  formLabelMuted: { fontSize: 11, fontWeight: '600', color: c.textSecondary },
  formIconCol: { width: 24, alignItems: 'center', justifyContent: 'center' },
  /** HR assignee: compact select trigger + absolutely positioned menu (no layout shift). */
  hrAssignSelectWrap: {
    position: 'relative',
    marginTop: 4,
    zIndex: 2,
  },
  hrAssignSelectWrapRaised: {
    zIndex: 40,
    elevation: 28,
  },
  hrAssignSelectBtn: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 999,
    backgroundColor: c.card,
    paddingHorizontal: 14,
    paddingVertical: 10,
    minHeight: 44,
    gap: 10,
  },
  hrAssignSelectBtnOpen: {
    backgroundColor: isDark ? '#172554' : '#dbeafe',
    borderColor: isDark ? '#3b82f6' : '#93c5fd',
  },
  hrAssignSelectBtnText: {
    flex: 1,
    fontSize: 13,
    color: isDark ? '#bfdbfe' : '#1e3a8a',
    fontWeight: '800',
  },
  hrAssignSelectBtnPlaceholder: {
    color: c.inputPlaceholder,
    fontWeight: '600',
  },
  hrAssignSelectMenu: {
    position: 'absolute',
    left: 0,
    right: 0,
    top: '100%',
    marginTop: 6,
    maxHeight: 240,
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 14,
    backgroundColor: c.card,
    zIndex: 50,
    elevation: 24,
    overflow: 'hidden',
    shadowcolor: c.text,
    shadowOffset: { width: 0, height: 10 },
    shadowOpacity: 0.14,
    shadowRadius: 18,
  },
  hrAssignSelectOption: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
    paddingHorizontal: 12,
    paddingVertical: 11,
    borderBottomWidth: StyleSheet.hairlineWidth,
    borderBottomColor: c.borderStrong,
  },
  hrAssignSelectOptionLast: {
    borderBottomWidth: 0,
  },
  hrAssignSelectOptionActive: { backgroundColor: isDark ? '#172554' : '#eef2ff' },
  hrAssignSelectOptionText: { fontSize: 13, color: c.textMuted, fontWeight: '600', flex: 1 },
  hrAssignSelectOptionTextActive: { color: isDark ? '#93c5fd' : '#1d4ed8', fontWeight: '800' },
  modalActions: { marginTop: 6, flexDirection: 'row', justifyContent: 'space-between', gap: 8 },
  cancelBtn: {
    borderWidth: 1,
    borderColor: c.borderStrong,
    borderRadius: 10,
    flex: 1,
    height: 42,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: c.inputBg,
  },
  cancelBtnText: { color: c.text, fontSize: 13, fontWeight: '700' },
  modalPrimaryBtn: {
    marginTop: 0,
    borderRadius: 10,
    backgroundColor: c.primaryMid,
    flex: 1,
    height: 42,
    alignItems: 'center',
    justifyContent: 'center',
  },
  attachmentField: {
    marginTop: 2,
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 12,
    padding: 10,
    backgroundColor: c.surfaceMuted,
  },
  attachmentPicker: {
    borderWidth: 1,
    borderStyle: 'dashed',
    borderColor: c.borderStrong,
    borderRadius: 10,
    backgroundColor: c.inputBg,
    paddingHorizontal: 8,
    paddingVertical: 8,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 10,
  },
  attachmentBtn: {
    borderRadius: 8,
    backgroundColor: isDark ? '#172554' : '#dbeafe',
    paddingHorizontal: 12,
    paddingVertical: 8,
  },
  attachmentBtnText: { color: isDark ? '#93c5fd' : '#1d4ed8', fontSize: 12, fontWeight: '700' },
  attachmentFileText: { flex: 1, minWidth: 0, fontSize: 12, color: c.textMuted, fontWeight: '600' },
  projectAttachmentRow: {
    marginTop: 8,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 10,
    borderRadius: 12,
    borderWidth: 1,
    borderColor: '#dbeafe',
    backgroundColor: '#eff6ff',
    paddingHorizontal: 10,
    paddingVertical: 8,
    minWidth: 0,
  },
  projectAttachmentIconWrap: {
    width: 32,
    height: 32,
    borderRadius: 10,
    backgroundColor: '#dbeafe',
    alignItems: 'center',
    justifyContent: 'center',
  },
  projectAttachmentTextWrap: { flex: 1, minWidth: 0 },
  projectAttachmentLabel: {
    fontSize: 10,
    fontWeight: '800',
    color: '#64748b',
    letterSpacing: 0.5,
    textTransform: 'uppercase',
  },
  projectAttachmentName: {
    marginTop: 2,
    fontSize: 12,
    fontWeight: '700',
    color: '#1d4ed8',
    minWidth: 0,
    flexShrink: 1,
  },
  projectAttachmentAction: {
    width: 28,
    height: 28,
    borderRadius: 8,
    backgroundColor: '#dbeafe',
    alignItems: 'center',
    justifyContent: 'center',
  },
  detailTitle: { fontSize: 18, fontWeight: '800', color: c.text, marginBottom: 6 },
  detailText: { fontSize: 13, color: c.textMuted, marginTop: 3, fontWeight: '600' },
  detailBody: { fontSize: 13, color: c.textMuted, marginTop: 6, lineHeight: 20 },
  taskDetailHeader: {
    marginHorizontal: -14,
    marginTop: -14,
    marginBottom: 12,
    paddingHorizontal: 16,
    paddingVertical: 15,
    backgroundColor: c.surfaceMuted,
    borderTopLeftRadius: 18,
    borderTopRightRadius: 18,
    borderBottomWidth: 1,
    borderBottomColor: c.borderStrong,
    flexDirection: 'row',
    gap: 10,
  },
  taskDetailTitleRow: { flexDirection: 'row', alignItems: 'center', gap: 8 },
  taskDetailHeaderTitle: { fontSize: 18, fontWeight: '900', color: c.text },
  taskDetailHeaderSub: { marginTop: 3, fontSize: 13, color: c.textMuted, fontWeight: '600' },
  taskDetailHeaderActions: { flexDirection: 'row', alignItems: 'center', gap: 6 },
  taskDetailActionBtn: {
    borderWidth: 1,
    borderColor: isDark ? '#1d4ed8' : '#bae6fd',
    backgroundColor: isDark ? '#172554' : '#f0f9ff',
    borderRadius: 999,
    width: 38,
    height: 38,
    alignItems: 'center',
    justifyContent: 'center',
  },
  taskDetailDeleteBtn: { borderColor: c.dangerBorder, backgroundColor: c.dangerBg },
  taskDetailBody: { flexDirection: 'row', gap: 10 },
  taskDetailMainCol: { flex: 1 },
  taskDetailAsideCol: { width: 112, alignItems: 'flex-end', gap: 10 },
  taskDetailBodyMobile: { flexDirection: 'column' },
  taskDetailAsideColMobile: { width: '100%', alignItems: 'flex-start' },
  taskDetailDueRow: { flexDirection: 'row', alignItems: 'center', gap: 6 },
  taskDetailDueText: { fontSize: 13, color: c.textMuted, fontWeight: '800', letterSpacing: 0.8 },
  taskDetailAttachmentCard: {
    marginTop: 10,
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 12,
    backgroundColor: c.surfaceMuted,
    paddingHorizontal: 12,
    paddingVertical: 10,
  },
  taskDetailAttachmentLabel: {
    fontSize: 11,
    fontWeight: '800',
    color: c.textSecondary,
    letterSpacing: 1,
    textTransform: 'uppercase',
  },
  taskDetailAttachmentRow: { marginTop: 8, flexDirection: 'row', alignItems: 'center', gap: 8 },
  taskDetailAttachmentName: { flex: 1, fontSize: 13, color: c.primaryLight, fontWeight: '700' },
  forwardTeamPill: {
    borderRadius: 999,
    backgroundColor: isDark ? c.surfaceElevated : '#e2e8f0',
    paddingHorizontal: 10,
    paddingVertical: 4,
  },
  forwardTeamPillText: { color: c.text, fontSize: 11, fontWeight: '800', letterSpacing: 0.4 },
  forwardWrap: {
    marginTop: 10,
    borderWidth: 1,
    borderColor: isDark ? c.infoBorder : '#c7d2fe',
    backgroundColor: isDark ? c.infoBg : '#eef2ff',
    borderRadius: 12,
    padding: 10,
    gap: 8,
  },
  forwardTitle: { fontSize: 12, fontWeight: '800', color: isDark ? c.infoText : '#3730a3' },
  forwardSelectWrap: { position: 'relative' },
  forwardSelectBtn: {
    height: 42,
    borderWidth: 1,
    borderColor: c.inputBorder,
    borderRadius: 10,
    backgroundColor: c.inputBg,
    paddingHorizontal: 12,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
  forwardSelectText: { fontSize: 13, color: c.text, fontWeight: '700' },
  forwardSelectMenu: {
    marginTop: 6,
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 12,
    backgroundColor: c.card,
    overflow: 'hidden',
    shadowcolor: c.text,
    shadowOffset: { width: 0, height: 8 },
    shadowOpacity: 0.16,
    shadowRadius: 16,
    elevation: 8,
  },
  forwardSelectOption: { paddingHorizontal: 12, paddingVertical: 9 },
  forwardSelectOptionActive: { backgroundColor: isDark ? '#172554' : '#eef2ff' },
  forwardSelectOptionText: { fontSize: 12, color: c.textMuted, fontWeight: '600' },
  forwardSelectOptionTextActive: { color: isDark ? '#93c5fd' : '#1e3a8a', fontWeight: '800' },
  startWorkBtn: {
    marginTop: 2,
    borderRadius: 10,
    backgroundColor: c.primaryMid,
    paddingVertical: 10,
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'row',
    gap: 6,
  },
  actionBtnDisabled: { opacity: 0.5 },
  projectLinkText: { marginTop: 4, fontSize: 11, color: '#2563eb', fontWeight: '600' },
  taskActionRow: { flexDirection: 'row', alignItems: 'center', gap: 8 },
  requestApproveBtn: {
    borderRadius: 10,
    backgroundColor: '#2563eb',
    paddingHorizontal: 12,
    paddingVertical: 8,
  },
  requestRejectBtn: {
    borderRadius: 10,
    backgroundColor: '#dc2626',
    paddingHorizontal: 12,
    paddingVertical: 8,
  },
  requestBtnText: { color: '#fff', fontSize: 12, fontWeight: '700' },
  assignCard: {
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 12,
    backgroundColor: '#f8fafc',
    padding: 12,
    marginBottom: 8,
  },
  serverTeamsRetry: {
    marginTop: 10,
    alignSelf: 'flex-start',
    backgroundColor: isDark ? '#172554' : '#eff6ff',
    borderWidth: 1,
    borderColor: isDark ? '#1d4ed8' : '#bfdbfe',
    borderRadius: 10,
    paddingHorizontal: 12,
    paddingVertical: 8,
  },
  serverTeamsRetryText: { fontSize: 13, fontWeight: '700', color: isDark ? '#93c5fd' : '#1d4ed8' },
  tlGroupCard: {
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 16,
    backgroundColor: c.card,
    padding: 12,
    marginBottom: 18,
    marginTop: 4,
    shadowcolor: c.text,
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.06,
    shadowRadius: 8,
    elevation: 2,
  },
  tlGroupBanner: {
    flexDirection: 'row',
    alignItems: 'flex-start',
    gap: 10,
    paddingBottom: 12,
    borderBottomWidth: 1,
    borderBottomColor: c.borderStrong,
    marginBottom: 10,
  },
  tlGroupBannerIcon: {
    width: 44,
    height: 44,
    borderRadius: 12,
    backgroundColor: isDark ? '#172554' : '#eff6ff',
    alignItems: 'center',
    justifyContent: 'center',
  },
  tlGroupLabel: { fontSize: 11, color: c.textMuted, fontWeight: '700', textTransform: 'uppercase', letterSpacing: 0.3 },
  tlGroupName: { fontSize: 17, fontWeight: '800', color: c.text, marginTop: 2 },
  tlTeamChips: { flexDirection: 'row', flexWrap: 'wrap', gap: 6, marginTop: 8 },
  tlTeamChip: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 4,
    backgroundColor: isDark ? '#172554' : '#eff6ff',
    borderWidth: 1,
    borderColor: isDark ? '#1d4ed8' : '#bfdbfe',
    borderRadius: 999,
    paddingHorizontal: 10,
    paddingVertical: 5,
  },
  tlTeamChipText: { fontSize: 12, fontWeight: '700', color: isDark ? '#93c5fd' : '#1d4ed8' },
  tlGroupCountPill: {
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: isDark ? c.surfaceElevated : '#f1f5f9',
    borderRadius: 12,
    paddingHorizontal: 10,
    paddingVertical: 8,
    minWidth: 52,
  },
  tlGroupCountNum: { fontSize: 18, fontWeight: '800', color: c.text },
  tlGroupCountLbl: { fontSize: 10, color: c.textMuted, fontWeight: '700' },
  taRosterTitle: { fontSize: 12, color: c.textMuted, fontWeight: '800', marginBottom: 8, textTransform: 'uppercase', letterSpacing: 0.5 },
  taMemberCard: {
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 14,
    backgroundColor: isDark ? c.surfaceElevated : c.surfaceMuted,
    padding: 12,
    marginBottom: 10,
  },
  taMemberTop: { flexDirection: 'row', alignItems: 'center', gap: 10 },
  taAvatar: {
    width: 44,
    height: 44,
    borderRadius: 22,
    backgroundColor: isDark ? '#172554' : '#dbeafe',
    alignItems: 'center',
    justifyContent: 'center',
    borderWidth: 1,
    borderColor: isDark ? '#1d4ed8' : '#bfdbfe',
  },
  taAvatarText: { fontSize: 18, fontWeight: '800', color: isDark ? '#93c5fd' : '#1d4ed8' },
  taMemberInfo: { flex: 1, minWidth: 0 },
  taMemberName: { fontSize: 16, fontWeight: '800', color: c.text },
  taRolePill: { borderRadius: 999, paddingHorizontal: 10, paddingVertical: 5, maxWidth: '40%', flexShrink: 0, marginTop: 2 },
  taRolePillLead: { backgroundColor: isDark ? '#2e1065' : '#e0e7ff' },
  taRolePillEmp: { backgroundColor: isDark ? '#052e16' : '#d1fae5' },
  taRolePillText: { fontSize: 11, fontWeight: '800' },
  taRolePillTextLead: { color: isDark ? '#c4b5fd' : '#4338ca' },
  taRolePillTextEmp: { color: isDark ? '#86efac' : '#047857' },
  taDetailGrid: { marginTop: 12, flexDirection: 'row', flexWrap: 'wrap', gap: 8 },
  taDetailCell: { width: '47%', flexGrow: 1, minWidth: 130 },
  taDetailCellFull: { width: '100%', marginTop: 2 },
  taDetailLabel: { fontSize: 11, color: c.textMuted, fontWeight: '700' },
  taDetailValue: { fontSize: 14, color: c.text, fontWeight: '700', marginTop: 2 },
  taDetailValueMono: { fontSize: 13, color: isDark ? '#93c5fd' : '#1d4ed8', fontWeight: '700', marginTop: 2 },
  taDeptRow: { flexDirection: 'row', alignItems: 'center', gap: 6, marginTop: 2 },
  teamsStatsRow: { flexDirection: 'row', gap: 10, marginBottom: 16 },
  teamsStatCard: {
    flex: 1,
    flexDirection: 'column',
    justifyContent: 'space-between',
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 16,
    backgroundColor: c.card,
    padding: 14,
    minHeight: 96,
    shadowColor: c.text,
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: isDark ? 0.12 : 0.06,
    shadowRadius: 8,
    elevation: 2,
  },
  teamsStatTopRow: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    width: '100%',
  },
  teamsStatIconWrap: {
    width: 36,
    height: 36,
    borderRadius: 10,
    alignItems: 'center',
    justifyContent: 'center',
  },
  teamsStatValue: { fontSize: 28, fontWeight: '800', color: c.text, lineHeight: 32 },
  teamsStatLabel: { fontSize: 12, fontWeight: '700', color: c.textMuted, marginTop: 12 },
  teamsSectionTitle: { fontSize: 18, fontWeight: '800', color: c.text, marginBottom: 12 },
  teamListCard: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 12,
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 16,
    backgroundColor: c.card,
    padding: 14,
    paddingBottom: 16,
    marginBottom: 10,
    shadowColor: c.text,
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: isDark ? 0.1 : 0.05,
    shadowRadius: 6,
    elevation: 2,
  },
  teamListIcon: {
    width: 48,
    height: 48,
    borderRadius: 14,
    alignItems: 'center',
    justifyContent: 'center',
  },
  teamListBody: { flex: 1, minWidth: 0, gap: 2 },
  teamListName: { fontSize: 16, fontWeight: '800', color: c.text },
  teamListMeta: { fontSize: 12, fontWeight: '600', color: c.textMuted, marginTop: 2 },
  teamListLeadRow: {
    flexDirection: 'row',
    alignItems: 'center',
    flexWrap: 'wrap',
    gap: 8,
    marginTop: 10,
  },
  teamListLeadPill: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 5,
    borderRadius: 999,
    borderWidth: 1,
    paddingHorizontal: 8,
    paddingVertical: 4,
  },
  teamListLeadDot: { width: 6, height: 6, borderRadius: 3 },
  teamListLeadPillText: { fontSize: 9, fontWeight: '800', letterSpacing: 0.5 },
  teamListLead: { fontSize: 12, fontWeight: '700', color: c.text, flexShrink: 1 },
  teamListChevron: {
    width: 32,
    height: 32,
    borderRadius: 16,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: isDark ? c.surfaceElevated : c.surfaceMuted,
    borderWidth: 1,
    borderColor: c.borderLight,
  },
  teamDetailHeaderWrap: { marginBottom: 16 },
  teamDetailHero: {
    borderRadius: 18,
    paddingHorizontal: 18,
    paddingTop: 18,
    paddingBottom: 32,
  },
  teamDetailHeroTitle: { fontSize: 22, fontWeight: '800', color: '#fff', marginTop: 10 },
  teamDetailHeroMeta: {
    fontSize: 13,
    fontWeight: '600',
    color: 'rgba(255,255,255,0.92)',
    marginTop: 6,
    marginBottom: 4,
  },
  teamLeadCard: {
    marginHorizontal: 10,
    marginTop: -24,
    zIndex: 2,
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 16,
    backgroundColor: c.card,
    padding: 14,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 12,
    shadowColor: c.text,
    shadowOffset: { width: 0, height: 3 },
    shadowOpacity: isDark ? 0.14 : 0.08,
    shadowRadius: 10,
    elevation: 3,
  },
  teamLeadBadge: {
    fontSize: 10,
    fontWeight: '800',
    color: c.textMuted,
    textTransform: 'uppercase',
    letterSpacing: 0.5,
  },
  teamLeadName: { fontSize: 16, fontWeight: '800', color: c.text, marginTop: 2 },
  teamLeadEmail: { fontSize: 12, fontWeight: '600', color: c.textMuted, marginTop: 3 },
  teamMembersTitle: { fontSize: 15, fontWeight: '800', color: c.text, marginBottom: 10 },
  teamMemberRow: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 10,
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 14,
    backgroundColor: isDark ? c.surfaceElevated : c.surfaceMuted,
    padding: 12,
    marginBottom: 8,
  },
  teamMemberName: { fontSize: 15, fontWeight: '800', color: c.text },
  teamMemberRole: { fontSize: 12, fontWeight: '600', color: c.textMuted, marginTop: 2 },
  teamEditBtn: {
    marginTop: 8,
    marginBottom: 20,
    minHeight: 50,
    borderRadius: 14,
    backgroundColor: c.primaryMid,
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'row',
    gap: 8,
  },
  teamEditBtnText: { fontSize: 15, fontWeight: '800', color: '#fff' },
  teamBackBtn: {
    width: 36,
    height: 36,
    borderRadius: 10,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: isDark ? c.surfaceElevated : c.surfaceMuted,
    borderWidth: 1,
    borderColor: c.borderLight,
  },
  teamFab: {
    position: 'absolute',
    right: 20,
    width: 56,
    height: 56,
    borderRadius: 28,
    backgroundColor: c.primaryMid,
    alignItems: 'center',
    justifyContent: 'center',
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.22,
    shadowRadius: 10,
    elevation: 10,
    zIndex: 20,
  },
  teamModalOverlay: {
    flex: 1,
    backgroundColor: c.modalBackdrop,
    justifyContent: 'flex-end',
  },
  teamModalSheet: {
    width: '100%',
    maxHeight: '88%',
    borderTopLeftRadius: 20,
    borderTopRightRadius: 20,
    backgroundColor: c.card,
    paddingHorizontal: 18,
    paddingTop: 14,
    paddingBottom: 24,
  },
  teamModalTitle: { fontSize: 18, fontWeight: '800', color: c.text },
  teamModalSub: { fontSize: 13, fontWeight: '600', color: c.textMuted, marginTop: 4, marginBottom: 14 },
  teamModalSection: { fontSize: 13, fontWeight: '800', color: c.text, marginTop: 8, marginBottom: 8 },
  teamCheckRow: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 10,
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 12,
    backgroundColor: isDark ? c.surfaceElevated : c.surfaceMuted,
    padding: 10,
    marginBottom: 8,
  },
  teamCheckRowActive: { borderColor: c.primaryMid, backgroundColor: isDark ? '#172554' : '#eff6ff' },
  teamCheckLabel: { flex: 1, minWidth: 0 },
  teamCheckName: { fontSize: 14, fontWeight: '800', color: c.text },
  teamCheckMeta: { fontSize: 11, fontWeight: '600', color: c.textMuted, marginTop: 2 },
  teamRemoveBtn: {
    width: 34,
    height: 34,
    borderRadius: 10,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: isDark ? '#450a0a' : '#fef2f2',
    borderWidth: 1,
    borderColor: isDark ? '#991b1b' : '#fecaca',
  },
  teamModalActions: { flexDirection: 'row', gap: 10, marginTop: 16 },
  teamModalPrimary: {
    flex: 1,
    minHeight: 46,
    borderRadius: 12,
    backgroundColor: c.primaryMid,
    alignItems: 'center',
    justifyContent: 'center',
  },
  teamModalPrimaryText: { fontSize: 14, fontWeight: '800', color: '#fff' },
  teamModalGhost: {
    flex: 1,
    minHeight: 46,
    borderRadius: 12,
    borderWidth: 1,
    borderColor: c.borderLight,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: isDark ? c.surfaceElevated : c.surfaceMuted,
  },
  teamModalGhostText: { fontSize: 14, fontWeight: '800', color: c.text },
  teamFormInput: {
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 12,
    backgroundColor: isDark ? c.surfaceElevated : c.surfaceMuted,
    paddingHorizontal: 12,
    paddingVertical: 10,
    fontSize: 14,
    fontWeight: '600',
    color: c.text,
    marginBottom: 10,
  },
  assignHeader: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' },
  assignName: { fontSize: 14, fontWeight: '800', color: c.text },
  assignId: { fontSize: 11, color: '#1d4ed8', fontWeight: '700' },
  assignMeta: { marginTop: 4, fontSize: 12, color: '#475569', fontWeight: '600' },
  assignTlRow: { marginTop: 10, flexDirection: 'row', flexWrap: 'wrap', gap: 6 },
  editBtn: {
    alignItems: 'center',
    justifyContent: 'center',
    width: 34,
    height: 34,
    borderRadius: 17,
    backgroundColor: c.primaryMid,
  },
  modalPreview: {
    marginTop: 4,
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 12,
    backgroundColor: '#f8fafc',
    padding: 10,
    gap: 6,
  },
  modalPreviewTitle: { fontSize: 12, fontWeight: '800', color: c.text },
  modalPreviewItem: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    borderWidth: 1,
    borderColor: c.borderStrong,
    borderRadius: 10,
    backgroundColor: c.card,
    paddingHorizontal: 10,
    paddingVertical: 8,
  },
  modalPreviewTask: { fontSize: 12, fontWeight: '700', color: c.text },
  modalPreviewSub: { fontSize: 11, color: '#64748b', marginTop: 2 },
  modalPreviewEdit: { fontSize: 11, fontWeight: '800', color: '#2563eb' },
  tlTimesheetPanel: {
    borderColor: '#d7def4',
    shadowcolor: c.text,
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.05,
    shadowRadius: 8,
    elevation: 1,
  },
  tlTimesheetTabsScroll: { marginHorizontal: -2, marginBottom: 10 },
  tlTimesheetTabs: {
    flexDirection: 'row',
    alignItems: 'center',
    borderBottomWidth: 1,
    borderBottomColor: '#e2e8f0',
    gap: 6,
    paddingRight: 6,
  },
  tlTimesheetTabBtn: {
    minWidth: 120,
    paddingVertical: 9,
    paddingHorizontal: 10,
    borderBottomWidth: 2,
    borderBottomColor: 'transparent',
    borderRadius: 10,
    alignItems: 'center',
  },
  tlTimesheetTabBtnActive: { borderBottomColor: '#4f46e5', backgroundColor: '#eef2ff' },
  tlTimesheetTabText: { fontSize: 12, color: '#64748b', fontWeight: '700' },
  tlTimesheetTabTextActive: { color: c.text, fontWeight: '800' },
  tlTimesheetWindowRow: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' },
  tlSummaryGrid: { flexDirection: 'row', flexWrap: 'wrap', gap: 10, marginTop: 10, marginBottom: 8 },
  tlSummaryCard: {
    flex: 1,
    minWidth: 96,
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 12,
    backgroundColor: c.card,
    paddingTop: 14,
    paddingHorizontal: 10,
    paddingBottom: 10,
    borderLeftWidth: 4,
  },
  tlSummaryCardHours: { borderLeftColor: '#2563eb' },
  tlSummaryCardOvertime: { borderLeftColor: '#7c3aed' },
  tlSummaryCardLate: { borderLeftColor: '#e11d48' },
  tlSummaryHead: { flexDirection: 'row', alignItems: 'center', gap: 6 },
  tlSummaryIconWrap: {
    width: 20,
    height: 20,
    borderRadius: 8,
    backgroundColor: '#eef2ff',
    alignItems: 'center',
    justifyContent: 'center',
  },
  tlSummaryLabel: { fontSize: 10, color: '#64748b', fontWeight: '800', lineHeight: 14 },
  tlSummaryValue: { marginTop: 6, fontSize: 28, fontWeight: '900', color: c.text },
  tlGdcNote: { marginBottom: 10, fontSize: 12, color: '#334155', fontWeight: '700' },
  timesheetCard: {
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 14,
    backgroundColor: '#f8fafc',
    padding: 12,
    marginBottom: 8,
  },
  timesheetTopRow: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' },
  timesheetName: { fontSize: 14, fontWeight: '800', color: c.text },
  timesheetDate: { fontSize: 11, color: '#64748b', fontWeight: '700' },
  timesheetId: { marginTop: 4, fontSize: 11, color: '#1d4ed8', fontWeight: '700' },
  timesheetTeam: { marginTop: 2, fontSize: 11, color: c.textMuted, fontWeight: '700' },
  timesheetClockRow: { marginTop: 10, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between' },
  timesheetClockPill: {
    flex: 1,
    borderWidth: 1,
    borderColor: '#bfdbfe',
    borderRadius: 10,
    backgroundColor: '#eff6ff',
    paddingVertical: 8,
    alignItems: 'center',
  },
  timesheetClockLabel: { fontSize: 10, color: '#1d4ed8', fontWeight: '800' },
  timesheetClockValue: { marginTop: 2, fontSize: 13, color: '#1e293b', fontWeight: '800' },
  timesheetMetaRow: { marginTop: 10, flexDirection: 'row', justifyContent: 'space-between', gap: 8 },
  timesheetMeta: { fontSize: 11, color: '#334155', fontWeight: '700' },
  timesheetLate: { color: '#dc2626' },
  timesheetOnTime: { color: '#16a34a' },
  timesheetStatusRow: { marginTop: 10, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between' },
  legendTitle: { fontSize: 11, color: '#64748b', fontWeight: '800', alignSelf: 'center' },
  statusCodePill: {
    minWidth: 28,
    height: 28,
    borderWidth: 1,
    borderColor: '#bfdbfe',
    borderRadius: 8,
    backgroundColor: '#eff6ff',
    alignItems: 'center',
    justifyContent: 'center',
  },
  statusCodeText: { fontSize: 11, color: '#1e3a8a', fontWeight: '800' },
  weekCellRow: { marginTop: 10, flexDirection: 'row', justifyContent: 'space-between', gap: 6 },
  weekCell: { alignItems: 'center', gap: 4, flex: 1 },
  weekCellDay: { fontSize: 10, color: '#64748b', fontWeight: '700' },
  recordFilterGrid: { gap: 10, marginBottom: 10 },
  recordField: { gap: 6 },
  recordFieldLabel: { fontSize: 12, color: isDark ? c.textMuted : '#64748b', fontWeight: '700' },
  recordChipWrap: { flexDirection: 'row', flexWrap: 'wrap', gap: 6 },
  availabilityCard: {
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 14,
    backgroundColor: '#f8fafc',
    padding: 12,
    marginBottom: 8,
  },
  availabilityTop: { flexDirection: 'row', alignItems: 'center', gap: 10 },
  availabilityAvatar: {
    width: 42,
    height: 42,
    borderRadius: 12,
    backgroundColor: '#dbeafe',
    alignItems: 'center',
    justifyContent: 'center',
  },
  availabilityAvatarText: { color: '#1d4ed8', fontSize: 16, fontWeight: '800' },
  availabilityName: { fontSize: 14, fontWeight: '800', color: c.text },
  availabilityMeta: { marginTop: 2, fontSize: 11, color: c.textMuted, fontWeight: '700' },
  availabilityStatusPill: { borderWidth: 1, borderRadius: 999, paddingHorizontal: 10, paddingVertical: 6 },
  availabilityPresent: {
    borderColor: isDark ? '#22c55e' : '#86efac',
    backgroundColor: isDark ? '#052e16' : '#dcfce7',
  },
  availabilityAbsent: {
    borderColor: isDark ? '#fbbf24' : '#fcd34d',
    backgroundColor: isDark ? '#422006' : '#fef3c7',
  },
  availabilityLeave: {
    borderColor: isDark ? '#fb7185' : '#fda4af',
    backgroundColor: isDark ? '#450a0a' : '#ffe4e6',
  },
  availabilityStatusText: {
    fontSize: 11,
    color: isDark ? c.text : '#334155',
    fontWeight: '800',
  },
  availabilityBottom: { marginTop: 10, flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' },
  availabilityId: { fontSize: 11, color: '#1d4ed8', fontWeight: '700' },
  availabilityActivity: { fontSize: 11, fontWeight: '800' },
  availabilityLogRow: {
    borderWidth: 1,
    borderColor: isDark ? c.borderLight : '#e5e7eb',
    borderLeftWidth: 3,
    borderLeftColor: '#f59e0b',
    borderRadius: 14,
    backgroundColor: isDark ? c.surfaceElevated : '#f8fafc',
    paddingHorizontal: 10,
    paddingVertical: 10,
    marginBottom: 6,
  },
  availabilityLogPresent: { borderLeftColor: '#16a34a' },
  availabilityLogAbsent: { borderLeftColor: '#f59e0b' },
  availabilityLogLeave: { borderLeftColor: '#e11d48' },
  availabilityLogHeader: { flexDirection: 'row', alignItems: 'center', gap: 10, marginBottom: 10 },
  availabilityDateBadge: {
    width: 58,
    borderWidth: 1,
    borderColor: c.borderStrong,
    borderRadius: 12,
    backgroundColor: c.card,
    paddingVertical: 6,
    alignItems: 'center',
    justifyContent: 'center',
  },
  availabilityDateDay: {
    fontSize: 10,
    color: isDark ? c.textMuted : '#94a3b8',
    fontWeight: '800',
    textTransform: 'uppercase',
  },
  availabilityDateNumber: { fontSize: 24, lineHeight: 26, color: c.text, fontWeight: '800' },
  availabilityDateMonth: { fontSize: 11, color: isDark ? c.textMuted : '#64748b', fontWeight: '700' },
  availabilityStatusBlock: { minWidth: 74, alignItems: 'center', justifyContent: 'center', gap: 4, flex: 1 },
  availabilityTodayText: {
    fontSize: 10,
    color: isDark ? c.primaryLight : '#4f46e5',
    fontWeight: '800',
    letterSpacing: 0.4,
  },
  availabilityMetricsGrid: { flexDirection: 'row', flexWrap: 'wrap', gap: 8 },
  availabilityMetricPill: {
    width: '48%',
    minHeight: 54,
    borderWidth: 1,
    borderColor: c.borderStrong,
    borderRadius: 12,
    backgroundColor: isDark ? c.surfaceMuted : c.card,
    paddingHorizontal: 10,
    justifyContent: 'center',
  },
  availabilityMetricLabel: {
    fontSize: 10,
    color: isDark ? c.textMuted : '#94a3b8',
    fontWeight: '800',
    letterSpacing: 0.6,
  },
  availabilityMetricValue: { marginTop: 3, fontSize: 20, lineHeight: 22, color: c.text, fontWeight: '800' },
  availabilityHoursPill: {
    backgroundColor: isDark ? '#172554' : '#eef2ff',
    borderColor: isDark ? '#1d4ed8' : '#c7d2fe',
  },
  availabilityLogHours: {
    marginTop: 3,
    fontSize: 20,
    color: isDark ? c.primaryLight : '#1e40af',
    fontWeight: '800',
  },
  availabilitySummaryGrid: { marginTop: 10, marginBottom: 10, flexDirection: 'row', flexWrap: 'wrap', gap: 8 },
  availabilitySummaryCard: {
    width: '48%',
    minWidth: 140,
    borderWidth: 1,
    borderRadius: 12,
    paddingHorizontal: 10,
    paddingVertical: 9,
    backgroundColor: c.card,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
  availabilitySummaryPresent: {
    borderColor: isDark ? '#166534' : '#bbf7d0',
    backgroundColor: isDark ? '#052e16' : '#f0fdf4',
  },
  availabilitySummaryAbsent: {
    borderColor: isDark ? '#92400e' : '#fde68a',
    backgroundColor: isDark ? '#422006' : '#fffbeb',
  },
  availabilitySummaryLeave: {
    borderColor: isDark ? '#9f1239' : '#fecdd3',
    backgroundColor: isDark ? '#450a0a' : '#fff1f2',
  },
  availabilitySummaryHours: {
    borderColor: isDark ? '#1d4ed8' : '#c7d2fe',
    backgroundColor: isDark ? '#172554' : '#eef2ff',
  },
  availabilitySummaryLabel: {
    fontSize: 10,
    color: isDark ? c.textMuted : '#64748b',
    fontWeight: '800',
    letterSpacing: 0.5,
    textTransform: 'uppercase',
  },
  availabilitySummaryValue: { fontSize: 22, color: c.text, fontWeight: '800' },
  dateSelectField: {
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 12,
    backgroundColor: c.card,
    paddingLeft: 12,
    paddingRight: 8,
    paddingVertical: 10,
    minHeight: 44,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
  dateSelectText: { flex: 1, color: c.text, fontSize: 14, fontWeight: '700' },
  dateSelectIconWrap: {
    marginLeft: 8,
    width: 28,
    height: 28,
    borderRadius: 8,
    backgroundColor: isDark ? '#172554' : '#eef2ff',
    alignItems: 'center',
    justifyContent: 'center',
  },
  requestCard: {
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 16,
    backgroundColor: c.card,
    padding: 14,
    marginBottom: 10,
    shadowcolor: c.text,
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.06,
    shadowRadius: 10,
    elevation: 2,
  },
  requestHeaderRow: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', gap: 10 },
  leaveTypeWrap: { position: 'relative', zIndex: 40, overflow: 'visible' },
  leaveTypeTrigger: {
    borderWidth: 1,
    borderColor: c.inputBorder,
    borderRadius: 12,
    backgroundColor: c.inputBg,
    paddingHorizontal: 12,
    minHeight: 40,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
  leaveTypeTriggerText: { fontSize: 14, color: c.text, fontWeight: '700' },
  leaveTypeMenu: {
    position: 'absolute',
    top: 46,
    left: 0,
    right: 0,
    borderWidth: 1,
    borderColor: c.inputBorder,
    borderRadius: 12,
    backgroundColor: c.card,
    overflow: 'hidden',
    zIndex: 50,
    shadowcolor: c.text,
    shadowOffset: { width: 0, height: 6 },
    shadowOpacity: 0.08,
    shadowRadius: 12,
    elevation: 3,
  },
  leaveTypeOption: { paddingHorizontal: 12, paddingVertical: 10, borderBottomWidth: 1, borderBottomColor: c.borderLight },
  leaveTypeOptionActive: { backgroundColor: isDark ? '#172554' : '#eef2ff' },
  leaveTypeOptionText: { fontSize: 13, color: c.textMuted, fontWeight: '600' },
  leaveTypeOptionTextActive: { color: isDark ? '#93c5fd' : '#1e3a8a', fontWeight: '800' },
  requestsRouteCard: {
    borderRadius: 16,
    borderWidth: 1,
    borderColor: c.borderStrong,
    backgroundColor: c.card,
    paddingHorizontal: 14,
    paddingVertical: 14,
    marginBottom: 10,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 12,
  },
  requestsRouteCardActive: {
    backgroundColor: '#eef2ff',
    borderColor: '#cbd5e1',
    borderLeftWidth: 4,
    borderLeftColor: '#cbd5e1',
  },
  requestsRouteIconWrap: {
    width: 34,
    height: 34,
    borderRadius: 10,
    backgroundColor: '#f8fafc',
    alignItems: 'center',
    justifyContent: 'center',
  },
  requestsRouteIconWrapActive: {
    backgroundColor: '#e2e8f0',
  },
  requestsRouteTitle: { fontSize: 16, fontWeight: '700', color: '#64748b' },
  requestsRouteTitleActive: { color: c.text },
  requestTopRow: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'flex-start', gap: 10 },
  requestName: { fontSize: 16, fontWeight: '800', color: c.text },
  requestDate: { fontSize: 12, fontWeight: '700', color: '#64748b', textAlign: 'right' },
  requestMeta: { marginTop: 4, fontSize: 12, color: '#475569', fontWeight: '700' },
  requestReason: { marginTop: 8, fontSize: 14, color: '#334155', lineHeight: 20 },
  rejectReasonBox: {
    marginTop: 8,
    borderWidth: 1,
    borderColor: '#fecaca',
    borderRadius: 10,
    backgroundColor: '#fef2f2',
    padding: 10,
  },
  rejectReasonTitle: { fontSize: 11, color: '#b91c1c', fontWeight: '800', textTransform: 'uppercase' },
  rejectReasonText: { marginTop: 4, fontSize: 12, color: '#7f1d1d', lineHeight: 18 },
  requestFooter: { marginTop: 12, flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' },
  requestTabsPanel: {
    borderBottomWidth: 1,
    borderBottomColor: '#e2e8f0',
    paddingBottom: 8,
    marginBottom: 2,
  },
  requestTabsBar: { flexDirection: 'row', gap: 20 },
  requestTabBtn: {
    paddingVertical: 8,
    borderBottomWidth: 3,
    borderBottomColor: 'transparent',
  },
  requestTabBtnActive: { borderBottomColor: '#4f46e5' },
  requestTabText: { fontSize: 14, color: '#64748b', fontWeight: '700' },
  requestTabTextActive: { color: c.text },
  requestTopControlsRow: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', gap: 10, marginBottom: 4 },
  requestTopActions: { alignItems: 'flex-end', gap: 10 },
  requestStatusSelectWrap: { minWidth: 122, position: 'relative', zIndex: 20 },
  requestStatusSelectInput: {
    borderWidth: 1,
    borderColor: c.inputBorder,
    backgroundColor: c.inputBg,
    borderRadius: 12,
    height: 40,
    paddingHorizontal: 12,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
  requestStatusSelectText: { fontSize: 14, fontWeight: '700', color: c.text },
  requestStatusMenu: {
    position: 'absolute',
    top: 44,
    right: 0,
    left: 0,
    backgroundColor: c.card,
    borderRadius: 12,
    borderWidth: 1,
    borderColor: '#cbd5e1',
    overflow: 'hidden',
  },
  requestStatusOption: { paddingHorizontal: 12, paddingVertical: 10, borderBottomWidth: 1, borderBottomColor: c.borderLight },
  requestStatusOptionActive: { backgroundColor: isDark ? '#172554' : '#eef2ff' },
  requestStatusOptionText: { fontSize: 13, color: c.textMuted, fontWeight: '600' },
  requestStatusOptionTextActive: { color: isDark ? '#93c5fd' : '#1e3a8a', fontWeight: '800' },
  modalPickerField: {
    borderWidth: 1,
    borderColor: c.inputBorder,
    borderRadius: 10,
    backgroundColor: c.inputBg,
    paddingHorizontal: 12,
    paddingVertical: 10,
    minHeight: 44,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
  modalPickerText: { color: c.text, fontSize: 13, fontWeight: '700' },
  requestCreateBtn: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 6,
    borderRadius: 14,
    paddingHorizontal: 14,
    paddingVertical: 11,
    backgroundColor: '#2563eb',
    shadowColor: '#1d4ed8',
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.2,
    shadowRadius: 8,
    elevation: 2,
  },
  requestCreateBtnText: { color: '#fff', fontSize: 14, fontWeight: '800' },
  myRequestsHeaderCard: {
    backgroundColor: c.card,
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 16,
    padding: 12,
  },
  myRequestsHeaderTop: { gap: 10 },
  myRequestsHeaderTitle: { fontSize: 18, fontWeight: '800', color: c.text },
  myRequestsTopActions: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', gap: 10 },
  myRequestsSelectWrap: { width: 132, position: 'relative', zIndex: 20 },
  myRequestsCreateBtn: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 6,
    borderRadius: 14,
    paddingHorizontal: 14,
    paddingVertical: 11,
    backgroundColor: '#2563eb',
    shadowColor: '#1d4ed8',
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.2,
    shadowRadius: 8,
    elevation: 2,
  },
  myRequestsCreateBtnIconOnly: {
    width: 44,
    height: 44,
    borderRadius: 12,
    paddingHorizontal: 0,
    paddingVertical: 0,
    justifyContent: 'center',
    gap: 0,
  },
  myRequestsCreateBtnText: { color: '#fff', fontSize: 14, fontWeight: '800' },
  myRequestsListWrap: {
    backgroundColor: c.card,
    borderWidth: 1,
    borderColor: c.borderLight,
    borderRadius: 16,
    padding: 10,
    marginTop: 8,
  },
  myRequestCard: {
    borderWidth: 1,
    borderColor: c.borderStrong,
    borderRadius: 14,
    backgroundColor: c.card,
    padding: 12,
    marginBottom: 10,
    borderLeftWidth: 4,
  },
  myRequestCardPending: { borderLeftColor: '#f59e0b' },
  myRequestCardApproved: { borderLeftColor: '#16a34a' },
  myRequestCardRejected: { borderLeftColor: '#dc2626' },
  myRequestTopRow: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'flex-start', gap: 10 },
  myRequestName: { fontSize: 16, fontWeight: '800', color: c.text },
  myRequestDate: { fontSize: 12, fontWeight: '700', color: '#64748b', textAlign: 'right' },
  myRequestMeta: { marginTop: 4, fontSize: 12, color: '#475569', fontWeight: '700' },
  myRequestReason: { marginTop: 8, fontSize: 14, color: '#334155', lineHeight: 20 },
  myRejectReasonBox: {
    marginTop: 8,
    borderWidth: 1,
    borderColor: '#fecaca',
    borderRadius: 10,
    backgroundColor: '#fef2f2',
    padding: 10,
  },
  myRequestFooter: { marginTop: 10, flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' },
  approvedChip: { backgroundColor: '#dcfce7', borderColor: '#86efac' },
  approvedChipText: { color: '#166534' },
  rejectedChip: { backgroundColor: '#fee2e2', borderColor: '#fca5a5' },
  rejectedChipText: { color: '#991b1b' },
  modalPrimaryBtnDisabled: { opacity: 0.55 },
  modalPrimaryBtnSuccess: { backgroundColor: '#059669', opacity: 1 },
  card: {
    backgroundColor: c.card,
    borderRadius: 22,
    borderWidth: 1,
    borderColor: c.borderLight,
    padding: 18,
  },
  title: { fontSize: 24, fontWeight: '800', color: c.text },
  sub: { marginTop: 6, fontSize: 14, color: c.textMuted, lineHeight: 22, marginBottom: 14 },
  box: {
    borderWidth: 1,
    borderColor: '#bfdbfe',
    borderRadius: 14,
    backgroundColor: '#eff6ff',
    padding: 14,
  },
  boxTitle: { fontSize: 14, fontWeight: '800', color: '#1e3a8a' },
  boxText: { marginTop: 6, fontSize: 13, lineHeight: 19, color: '#334155' },
});
}

